# 바이너리 / 캐시
__pycache__/
*.py[cod]
*$py.class

# 가상환경
.venv
venv/
env/
.conda/
.python-version

# 테스트 / 커버리지
htmlcov/
.coverage
.coverage.*
.cache
coverage.xml

# IDE / 에디터 설정
.vscode/
.idea/
*.iml

# 로그 / 기타
*.log
*.tmp
*.DS_Store
Thumbs.db

# 런타임 산출물
runs/
# binjari_setup 스킬이 머신별로 생성 (경로가 PC 마다 다름)
launch_binjari.bat

# env 실값 전체 차단 (로그인·카드·Azure 시크릿) — 양식(example)만 커밋 허용
.env
.env.*
!.env.*.example
!.env.example

# 인증 DB (OAuth 토큰 등 비밀 포함) + 토큰 암호화 키
*.db
*.db.key

# Claude Code 로컬 설정 (개인 권한/환경)
.claude/settings.local.json
